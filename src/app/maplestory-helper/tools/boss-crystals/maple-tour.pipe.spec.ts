import { MapleTourPipe } from './maple-tour.pipe';

describe('MapleTourPipe', () => {
  it('create an instance', () => {
    const pipe = new MapleTourPipe();
    expect(pipe).toBeTruthy();
  });
});
